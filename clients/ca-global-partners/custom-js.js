(function(){ document.addEventListener('DOMContentLoaded', function() { const mobileToggle = document.querySelector('.cstm-mobile-hamburger-menu'); const mobileMenu = document.querySelector('.mobile-right-side-menu'); if (mobileToggle && mobileMenu) { mobileToggle.addEventListener('click', function() { const isOpen = mobileMenu.style.transform === 'translateX(0px)' || mobileMenu.style.transform === 'translateX(0%)'; mobileMenu.style.transform = isOpen ? 'translateX(100%)' : 'translateX(0%)'; }); } const desktopDropdowns = document.querySelectorAll('.desktop-navmenu-about, .desktop-navmenu-help'); desktopDropdowns.forEach(function(dropdown) { const submenu = dropdown.querySelector('ul'); if (submenu) { dropdown.addEventListener('mouseenter', function() { submenu.style.display = 'block'; submenu.style.opacity = '1'; submenu.style.transform = 'translateY(0)'; }); dropdown.addEventListener('mouseleave', function() { submenu.style.opacity = '0'; submenu.style.transform = 'translateY(-10px)'; setTimeout(function() { submenu.style.display = 'none'; }, 200); }); } }); }); })();