(function(){ document.addEventListener('DOMContentLoaded', function() { const hamburger = document.querySelector('.cstm-mobile-hamburger-menu'); const mobileMenu = document.querySelector('.mobile-right-side-menu'); if (hamburger && mobileMenu) { hamburger.addEventListener('click', function() { mobileMenu.classList.toggle('active'); }); document.addEventListener('click', function(e) { if (!hamburger.contains(e.target) && !mobileMenu.contains(e.target)) { mobileMenu.classList.remove('active'); } }); } const dropdownTriggers = document.querySelectorAll('.desktop-navmenu-about, .desktop-navmenu-help'); dropdownTriggers.forEach(function(trigger) { const dropdown = trigger.querySelector('.dropdown-menu'); if (dropdown) { trigger.addEventListener('mouseenter', function() { dropdown.style.display = 'block'; }); trigger.addEventListener('mouseleave', function() { dropdown.style.display = 'none'; }); } }); }); })();